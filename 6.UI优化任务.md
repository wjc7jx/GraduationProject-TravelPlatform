# UI 优化任务记录

> 本文件记录旅行项目管理小程序（TripTimeline）的 UI/UX 优化背景、设计决策与任务拆分，供后续开发迭代参考。

---

## 一、项目背景

**项目名称**：TripTimeline —— 旅行项目管理微信小程序（毕业设计）

**核心流程**：先创建旅行项目（project） → 再在项目下创建记录条目（entry）

**当前核心页面**：
- `pages/project-editor`：新建 / 编辑旅行项目（表单页）
- `pages/editor`：新建 / 编辑旅行记录（表单页）

---

## 二、设计语言规范

**视觉主题**："Cartographer's Journal"（测绘学家的复古日记）/ 现代实体杂志风

**色彩系统（大地的回音）**：

| 角色 | 色值 | 用途 |
|------|------|------|
| 背景基色 Base | `#F7F5F0` | 全局页面背景，羊皮纸暖米色 |
| 主色调 Primary | `#2A4B3C` | 重要按钮、强调文字、已选标签 |
| 点缀色 Accent | `#C85A3D` | 地理标记、高亮、警告、装饰线 |
| 主文字 | `#1C1C1C` | 正文标题，近黑深木炭色 |
| 次要文字 | `#8A877E` | 标签、副标题、提示文字 |

**排版原则**：
- 大量留白，打破等宽列表惯例
- 卡片设计可不对称，参考拍立得 / 火车票风格
- 标题区使用较大字号（44rpx+），副信息用 24rpx

**动效原则**：
- 页面进入：阶梯式上浮淡入（Staggered fade-up）
- 按钮交互：`scale(0.98)` press 反馈
- 状态切换：`transition: all 0.2s`

---

## 三、设计决策记录

以下为与 AI 协作审查时确认的设计决策，与初始建议有出入的以此为准：

| 议题 | 决策 | 原因 |
|------|------|------|
| 目的地字段 | ❌ 不新增 | 融入标题即可，如"重庆一日游" |
| 旅行人数字段 | ❌ 不新增 | 当前阶段不需要 |
| 标签（旅行印记）位置 | ✅ 保持现有位置（日期后） | 用户在计划初期就会想到标签 |
| 副标题字段 | ✅ 保留 | 定位为旅行的额外补充描述 |
| 心情 / 天气字段 | ❌ 不新增 | 写入记录正文即可 |
| editor 类型切换 | ✅ 保留，但重构 | 文字为必有项（不纳入选择），移除轨迹，保留图片/音频 |
| 轨迹（GPX）功能 | ❌ 移除 | 不需要直接导入 GPX 轨迹文件 |

---

## 四、任务拆分

### Task 1：`project-editor` UI & 交互优化 ✅ 已完成

**变更文件**：`project-editor.wxml` / `project-editor.scss` / `project-editor.ts`

| 子项 | 内容 | 状态 |
|------|------|------|
| 封面空状态 | 加虚线边框 + 斜向纹理背景（`repeating-linear-gradient`） | ✅ |
| 封面进度角标 | 有图时右下角显示 `N/3` 毛玻璃角标 | ✅ |
| form-card 视觉 | 背景从纯白改为 `#FDFCF8`，顶部加陶土红 `#C85A3D` 装饰条（`::before`） | ✅ |
| 标签预设交互 | 从"点击消失"改为 toggle active 态（`✓` 前缀 + 浅绿高亮），点击已选则取消 | ✅ |
| 标签自定义输入 | 移除"+ 自定义"独立按钮，展开时常驻显示输入框 | ✅ |
| 提交按钮三态 | `idle` / `loading` / `success`（✓ 已保存）/ `error`（陶土红 + shake 抖动） | ✅ |
| 全页文案替换 | 见下方文案对照表 | ✅ |

**文案对照（project-editor）**：

| 原文案 | 新文案 |
|--------|--------|
| 编辑旅行日记 | 编辑旅程 |
| 开启新旅程（导航栏）| 记录新旅程 |
| 给这次旅行起个名字... | 这次旅程叫什么？ |
| 一句话记录当下的心情或目标... | 出发前，你想说一句话... |
| 出发日 | 何时出发 |
| 结束日(选填) | 何时归来 |
| 至（日期分隔符）| → |
| 旅行印记（标签行标签）| 这趟旅行是... |
| 灵感: | 常见印记 |
| 谁能看到我们的回忆 | 谁可以看到这本日记 |
| 🚀 开启旅程 | 出发 → |
| 保存旅行纪事 | 保存 |

---

### Task 2：`editor` 结构重构 ✅ 已完成（含接口字段同步）

**变更文件**：`editor.wxml` / `editor.scss` / `editor.ts` / 后端接口字段

| 子项 | 内容 | 状态 |
|------|------|------|
| 文字区域必有化 | `textarea` 始终渲染，不受类型选择控制 | ✅ |
| 类型选择器重构 | 移除"文字"和"轨迹"选项，仅保留"图片"和"音频"作为可附加媒体 | ✅ |
| 接口字段同步 | `contentType` 枚举清理，删除 `trackFileName`、`trackPointCount` 等字段 | ✅ |
| 地点模块重构 | 三个输入框收拢为单一"📍 添加地点"入口（搜索 + 地图 + 手动折叠） | ✅ |
| 日期时间合并 | 合并为单行，进入页面自动填当前时间 | ✅ |
| 提交按钮三态 | 与 project-editor 保持一致的三态逻辑 | ✅ |
| 全页文案替换 | 见下方文案对照表 | ✅ |

**文案对照（editor）**：

| 原文案 | 新文案 |
|--------|--------|
| 记录你的旅行瞬间 | 此刻，值得记录 |
| 输入你的旅行记录内容 | 写下当时的感受... |
| 标题（可选）| 给这个瞬间一个名字 |
| 手动输入地点名称 | 你在哪里？ |
| 选择图片并自动识别 EXIF | 添加照片，自动带入时间地点 |
| 录音中 Xs / 60s | 正在收录环境音... X″ |
| 继承项目（隐私）| 跟随日记本 |
| 保存（提交按钮）| 记下这一刻 |

---

### Task 3：文案统一替换 ✅ 已完成（与 Task 2 合并执行）

Task 2 执行时已一并完成 editor 的文案替换，保留该项用于 checklist 核查。

---

## 六、Task 2/3 执行补充说明（2026-04-13）

- 已完成前后端 `content_type` 枚举收敛：移除 `track`，保留 `photo` / `note` / `audio`。
- 已清理导出链路与详情页轨迹预览分支，避免前后端协议不一致。
- 已补齐 `editor` 提交按钮 `loading/success/error` 的样式态与 `shake` 动效。
- 备注：`project-editor` 的副标题目前仍为前端展示字段，后端尚无独立持久化字段；如需持久化，建议新增项目字段并补迁移脚本。

---

## 五、参考产品

- **小红书**：情绪化文案、标签交互
- **飞猪**：日期选择联动体验
- **Notion**：极简信息架构、模块化布局
- **《Kinfolk》杂志**：留白、纸质感视觉语言
